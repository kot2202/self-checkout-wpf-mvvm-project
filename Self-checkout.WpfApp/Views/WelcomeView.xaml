<UserControl x:Class="Self_checkout.WpfApp.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Self_checkout.WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640" Background="Beige">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="1" Content="Next screen" Command="{Binding NavigateCheckoutCommand}" VerticalAlignment="Bottom" Height="115" Margin="16,0,0,0"/>
    </Grid>
</UserControl>
